# Team implementation progress

Requirements: [team-requirements.md](team-requirements.md). Approved for implementation; preserve main's unrelated untracked files.

## Continuation
- Delivery: user authorized commit and push to `origin/main`. All team changes, migrations and project documentation are included; unrelated Windows cache files remain untracked. Runtime secrets and local preview configuration are excluded.
- [x] T13: Restyled team overview, keys and settings to the supplied flat/cyan reference. Compact member rows expand to reveal existing usage/limits/actions; invitation form, count and empty state are independent. Added quick guide and responsive/dark styling. Targeted component/locale tests (13), typecheck, lint and production build passed; desktop/mobile mock-browser verification covers member controls, guide, keys, settings and dark mode. Existing preview at port 5186 displays the updated UI; backend remains running at 8080.
- [x] T7: Durable known-cost billing retry, owner-only recovery action, pending-state visibility, no further member admissions with unsettled bills, and fail-closed unknown usage. Database failure/duplicate-retry and usage repair regression passed.
- [x] T8: Real PostgreSQL failure/retry, concurrent idempotency, usage-log repair, unpaid-member admission guard and unknown-usage retention passed. Ten frontend component/locale tests, typecheck, lint and build passed; targeted Go regression, build and vet passed. Contracts and AGENTS updated. Frontend-only preview: http://127.0.0.1:5186/team (backend not launched).
- [ ] T9 (cancelled by user): No Live/video/image generation integration; all excluded from team scope.
- [x] T10: Text-only admission covers HTTP media endpoints, image models/tools, Gemini response modalities, duplicate keys and WS turns; team model discovery filters image models. Personal-key behavior and image inputs for text analysis stay unchanged. Service/middleware tests passed.
- [x] T11: Database-configured frontend URL gates send/resend before token rotation; editable bilingual team.invitation templates use existing mail system. Local SMTP tests verify links, escaping, hashes and rotation. Browser mock checks confirm token survives login without query exposure and acceptance requires a click; session storage also covers registration's dashboard redirect.
- [x] T12: Platform-admin full-key access, name/group/limit edits, member removal, invite revocation, pause/resume, safe dissolution and billing recovery. Shared TeamView/API contracts with server-resolved, ID-scoped admin context preserve the audit actor. Domain/template configuration and paginated search/list added. Unit and real PostgreSQL tests verify permissions, stale ID isolation and settlement safeguards; desktop/mobile mock-API screenshots show no page overflow.

- [x] T1: Trace auth, subscription routing, usage billing, asynchronous settlement and API/UI patterns; record technical design. Evidence: Trellis research/backend-architecture.md and docs/team-api.md.
- [x] T2: Implement team persistence/API, lifecycle, invitations, keys and permissions; disposable PostgreSQL integration passed.
- [x] T3: Owner-funded group routing, quotas and settlement for HTTP/WS text; targeted routing/personal regressions and real database billing checks passed. Media generation was subsequently excluded by the user and is rejected, not pending implementation. Unknown crashed admission reconciliation remains manual.
- [x] T4: Creation/overview/keys/settings UI, navigation and locales. Typecheck, lint, build and eight component/locale tests passed.
- [x] T5: Independent review fixed membership/key-creation race and scoped audit cleanup; root fixed cached key re-enable, midnight/reset settlement, and synchronous team usage writes. Targeted tests, database integration, frontend checks passed; full service suite has baseline failures described in docs/team-billing.md.
- [x] T6: Project contract and AGENTS index updated. No commit/push/deployment/live migrations.

Recovery: main checkout D:/llm/sub2api-multi-subscriptions. Trellis coordination task D:/llm/sub2api/.trellis/tasks/09-16-team-billing (metadata only in historical checkout). Core implementation/check complete; see handoff boundary below. Existing PostgreSQL multi-subscription migration/concurrency regression passed on an isolated Docker database. Disposable container sub2api-team-test-20260916 (loopback port 55439) is stopped after verification; restart only for isolated tests, never use application data.

Quality checkpoint: targeted team/personal all-subscription/delete tests, Redis scoped image-task cleanup and disposable PostgreSQL tests passed. Earlier full handler/server/middleware/routes/repository suite passed. Earlier full service suite failed on Ollama stale callback (also reproduced from pristine main) and a moderation timing assertion (passed isolated); this continuation reran targeted regression, not the full service suite. Daily member reset follows configured local midnight; week/month windows are 7/30 days. Dissolve refuses pending requests, then removes team keys/usage/dedup/membership/invitations/key-linked audits and cached image tasks. Recorded failed bills now have owner recovery; crash-before-close and unknown usage still require reconciliation. External generated image files remain under object storage retention.

Handoff boundary: confirmed text-only team feature, known-cost failed-billing recovery, administrator management and templated domain-based invitation links are implemented. Frontend typecheck/lint/build and 16 targeted component/utility/locale tests passed. Backend targeted regressions/build/vet and isolated PostgreSQL checks passed; full service suite was not rerun (previous baseline failures are in team-billing.md). Desktop/mobile browser checks use mocked APIs, not a live backend. Frontend preview remains http://127.0.0.1:5186/admin/teams; backend was not started. No real emails, live migrations, deployment, commit or push. Unknown/crashed admission reconciliation is not automatic; video/Live/image generation are excluded rather than unfinished.
